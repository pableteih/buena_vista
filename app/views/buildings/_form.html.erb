<%= form_with(model: building) do |form| %>
  <% if building.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(building.errors.count, "error") %> prohibited this building from being saved:</h2>

      <ul>
        <% building.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :Nombre, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :Calle, style: "display: block" %>
    <%= form.text_field :street %>
  </div>

  <div>
    <%= form.label :Numeración, style: "display: block" %>
    <%= form.number_field :number %>
  </div>

  <div>
    <%= form.label :comuna_id, style: "display: block" %>
    <%= form.collection_select :comuna_id, Comuna.all, :id, :name, prompt: "Selecciona una comuna" %>  </div>
  <div>

  <div class="field">
    <%= form.label 'Selecciona los servicios disponibles' %>
    <% Service.all.each do |service| %>
      <div class="form-check">
        <%= form.check_box :service_ids, { multiple: true }, service.id, nil %>
        <%= form.label service.name, class: 'form-check-label' %>
      </div>
    <% end %>
  </div>




    <%= form.submit %>
  </div>
<% end %>
